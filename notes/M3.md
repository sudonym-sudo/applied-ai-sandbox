# M3 — Context Engineering

_Cohort notes for the M3 module. Edited as I work through it._

## Step 2 — CLAUDE.md behaviors

- It tells Claude to run `pytest` after each change, so Claude doesn't ship code that doesn't actually pass the repo tests.

- tells not to edit the tests folder so it doesn't change the goals. 

## Step 3 — bad-context run

It did it all right because I put my agent in plan mode and it asked me these questions :

​Q: Should login be a simple hard-coded user/password form, or do you expect user registration and multiple users?
​A: Simple hard-coded login - One fixed username/password stored in code or config.
​Q: Which app actions should require login?
​A: Only note creation/deletion - View homepage anonymously, but editing and deleting require login.
​Q: Should the logged-in state use Flask session cookies?
​A: Yes, use Flask session - Store login state in session and show logout link.

## Step 4 — good-context plan

## Plan

### 1. Add `models.py`
- Create a new `models.py` module with:
  - a `User` class implementing `flask_login.UserMixin`
    - SQLite helper functions to:
        - initialize the `users` table
            - find a user by `id`
                - find a user by `username`
                    - create a new user with `werkzeug.security.generate_password_hash`
                        - verify a password with `check_password_hash`
                        - Keep user storage in a real SQLite table, as requested.

                        ### 2. Update app.py
                        - Import and wire up Flask-Login:
                          - `LoginManager`
                            - `login_user`
                              - `logout_user`
                                - `login_required`
                                  - `current_user`
                                  - Initialize `LoginManager`, set `login_view = "login"`, and add the `user_loader`.
                                  - Call the user DB initialization when the app is created.
                                  - Add auth routes:
                                    - `GET/POST /register`
                                      - `GET/POST /login`
                                        - `GET /logout`
                                        - Protect the main app routes:
                                          - redirect anonymous visitors from `/` to `/login`
                                            - require login for note creation and likely the homepage
                                            - Keep the existing note schema intact:
                                              - preserve `app.notes` as-is for compatibility with existing tests
                                                - add a new per-user note store such as `app.user_notes` keyed by `current_user.id`
                                                  - render only the current user’s notes on `/`
                                                    - when a note is created, add it to the current user’s note list
                                                    - Ensure `/logout` logs the user out and redirects to `/login`.

                                                    ### 3. Add templates
                                                    - Add `templates/login.html`
                                                    - Add `templates/register.html`
                                                    - Add a shared navigation area:
                                                      - either create `templates/base.html` and extend it
                                                        - or add nav links directly in the templates
                                                        - Ensure the nav changes based on authentication state:
                                                          - logged-out: show `Login` / `Register`
                                                            - logged-in: show `Home` / `New note` / `Logout`

                                                            ### 4. Preserve existing tests
                                                            - Because current tests already inspect `app.notes` and post to `/notes/new`, I’ll keep `app.notes` as a list to avoid breaking them.
                                                            - Add `app.user_notes` or equivalent for per-user note isolation so the UI behavior is correct without changing the existing note structure.
                                                            - If necessary, update conftest.py to log in a default test user automatically during tests so the auth requirement does not break the current task tests.

                                                            ### 5. Verification
                                                            - Run `pytest` after implementation.
                                                            - Confirm:
                                                              - `/register` and `/login` render correctly
                                                                - `/logout` works
                                                                  - anonymous `/` redirects to `/login`
                                                                    - different users see different notes
                                                                      - existing tests still pass

                                                                      If you want, I can next implement the routes, models, and templates with this exact structure.

## Step 5 — comparison

- Biggest effect: “Because current tests already inspect `app.notes` and post to `/notes/new`, I’ll keep `app.notes` as a list to avoid breaking them.” That line was the one that would keep Claude from changing the existing note flow.
- Ignored line: “Keep user storage in a real SQLite table, as requested.” The bad-context run still went for a hard-coded login/session approach instead of the DB-backed user model.
- Missing context: I should have said more clearly that the current repo is just a tiny note app where `app.notes` is the only data store and `tests/test_task_01.py` checks `/notes/new` with no auth. That would make the “add login without breaking existing notes” constraint much stronger.
