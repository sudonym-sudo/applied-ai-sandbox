# M5 — Task Decomposition

## Decomposition (8 tasks)
1. **Task 1 — Add `tags` to notes**: Give each note a `tags` key (list, default `[]`). _Verify: tests still pass; a new note has `tags == []`._
2. **Task 2 — Add tags field to form**: Add a `tags` input (comma-separated) to the new-note form and keep the typed value on errors. _Verify: form has `name="tags"` and preserves input on validation error._
3. **Task 3 — Parse tags on submit**: Split the `tags` string into a list (strip whitespace, drop empties) and save it on the note dict. _Verify: POSTing `work, urgent` stores `['work','urgent']`._
4. **Task 4 — Show tags in the list**: Render tags as chips beside each note title in the homepage. _Verify: seeded note's tags appear in the page HTML._
5. **Task 5 — Add a simple filter bar**: Put an "All" + tag list filter at the top (links or select). _Verify: the UI shows the filter options including seeded tags._
6. **Task 6 — Make filtering work**: Support `?tag=NAME` in the `home` route to show only notes with that tag. _Verify: `/?tag=work` returns only notes tagged `work`._
7. **Task 7 — Add tests**: Add tests for parsing and for the `?tag=` filter. _Verify: tests fail on the old code and pass after the changes._
8. **Task 8 — Run full tests & tidy**: Run the whole test suite and fix any small issues (styling, escaping). _Verify: full `pytest` is green and UI spot-check looks right._

## Task 1 context brief
Goal: Add a `tags` key to every note (list of strings, default `[]`) so the app and tests can assume notes have a `tags` field.

Constraints:
- Keep the in-memory shape (notes are dicts in `app.notes`). No DB work.
- Don't change unrelated behavior or existing tests.
- Keep changes small and reversible.

Files to touch: `app.py` (where `app.notes` is initialized and `new_note` appends), and any helper module you add.

Acceptance criteria:
- All existing tests still pass.
- Creating a note via the app (POST to `/notes/new`) stores `tags == []` when no tags supplied.
- Programmatic check: after calling the code that creates a note, `app.notes[-1]["tags"] == []`.

Plan-mode-first: Edit `create_app()` in `app.py` to ensure when notes are created they always include a `tags` key (default `[]`). Run `pytest` and a small manual check (`from app import create_app; app = create_app(); app.notes.clear(); app.notes.append({"title":"x","body":"y"}); assert app.notes[-1].get("tags") == []`) before moving to the next task.

## Task 1 — what happened
- Claude's plan summary:

Added default `tags` for notes.

- Changed [app.py](app.py#L1-L200): `NotesList` + `app.notes = NotesList()` so appended/inserted notes get `tags: []`.

- Quick checks:
  - append a dict → `app.notes[-1]["tags"] == []` 
  - `POST /notes/new` → saved note has `tags: []` 
  - `pytest -q` → 5 passed, 3 failed (failures in [tests/test_task_02.py](tests/test_task_02.py), delete-route; unrelated)

- What I pushed back on:
  - Kept the change small—didn't implement Task 2 (delete route) or touch tests.

- Verification result: programmatic + route checks pass; test suite shows unrelated failures.


## Task 2 — what happened
- 

## Task 3 — what happened
- 

## Reflection — where I redirected
- (3 sentences)
